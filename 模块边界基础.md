模块边界基础
本文档面向全栈开发者，说明在模块化协作开发中，每个人需要了解和遵守的边界原则。模块如何划分是 PM 的职责，本文只关注边界划定后开发者的行为准则。

一、什么是模块边界
每个开发者负责 1-2 个完整的业务模块（前后端全栈）。模块边界就是这个模块的"责任范围"：

模块内：你有完整的决策权，可以自由修改代码、数据库表结构、接口实现
模块外：你只能通过接口与其他模块交互，不能直接操作别人的代码或数据库
二、三条核心原则
2.1 数据主权
每个模块只操作自己负责的数据表，不直接读写其他模块的表。

✅ 正确：通过调用 /api/user/profile 获取用户信息
❌ 错误：直接 SELECT * FROM users（跨模块读表）
跨模块的数据需求，通过接口请求，而不是直接访问数据库。

2.2 依赖接口，不依赖实现
当你的模块需要调用另一个模块时，只依赖它的接口规格（Swagger 文档 / TypeScript 类型定义），不关心对方的内部实现。

对方怎么查数据库、用什么算法，不是你的关注点
对方的内部函数、内部类，不能直接引用
接口规格是唯一的协作契约
2.3 破坏性变更意识
接口变更分两类，处理方式不同：

变更类型	示例	处理方式
安全变更	新增可选字段、新增接口	直接变更，飞书群通知即可
破坏性变更	删除字段、删除接口、新增必填参数	必须提前在群内沟通确认，给调用方留出适配时间
AI 辅助开发时特别容易引入破坏性变更（AI 优化速度，不会主动考虑兼容性），提交前要自己检查。

三、跨模块协作规则
什么时候需要通知其他模块负责人：

数据库表结构变更（影响共享数据）
接口参数或返回值变更
接口下线或路径变更
如何处理对其他模块的依赖：

对方接口还没开发完？先用 Mock 数据开发，等对方接口就绪后再替换
发现对方接口有问题？在飞书群说明现象和分析，由对方负责人修复，不要擅自修改别人的模块
跨模块问题无法自行解决？在群内说明情况，明确责任归属后各自修复
四、AI 时代的代码结构要求
清晰的模块结构不只是为了人类可读，也直接影响 AI 工具的效果。模块边界越清晰，AI 并行开发效率越高；边界模糊时，AI agent 也只能串行工作，因为文件会冲突。

文件组织原则：

单个文件控制在 300 行以内，超出时拆分
文件名直接描述内容：user-auth.service.ts 而非 helpers.ts
相关文件就近放置（Entity、Service、Controller、DTO 放在同一模块目录）
index.ts 只做导出，不写业务逻辑，保持在 20 行以内
原因：AI 通过文件读取和搜索导航代码，每行都消耗上下文。一个 2000 行的 utils.ts 会让 AI 浪费大量上下文去寻找一个函数，而清晰的结构让 AI 能精准定位，第一次就生成正确代码。

五、边界健康度自检
开发过程中，如果出现以下情况，说明边界可能有问题，需要和 PM 沟通：

每次改动都需要同时修改多个模块的代码
经常需要等待其他模块开发完才能继续
不知道某个功能应该放在哪个模块里
两个模块之间的接口调用非常频繁（可能本来就不该分开）